# ML_Trees_02 script
# This is the second script for the Machine Learning R workshop
#  This script assumes you loaded the 
#  dataset using the 'import dataset' button (GUI)
#  This script assumes you have installed the packages rpart and 
#    rpart.plot
# MR 4/20/18

# First install the packages rpart and rpart.plot using the 
#  packages tab from on the plot menu (lower right frame)
3+5 # I put this in here because I wanted to make sure it worked
# When everything was 'truncated', I could not see what was going on


library(rpart.plot)
library(rpart)